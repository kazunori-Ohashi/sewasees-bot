# Discord Bot TDD Implementation - Environment Variables Template
# Copy this file to .env and fill in your actual values

# Discord Bot Token (required)
# Get this from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# OpenAI API Key (required)
# Get this from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Redis Configuration (optional - defaults to localhost)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Optional: Redis Password (if your Redis instance requires authentication)
# REDIS_PASSWORD=your_redis_password

# Optional: Custom rate limit for free users (default: 5)
# DAILY_RATE_LIMIT=5

# Optional: Premium role name (default: premium)
# PREMIUM_ROLE_NAME=premium

# Optional: Moderator log channel ID (for admin notifications)
# MODERATOR_CHANNEL_ID=123456789012345678

# Optional: Enable debug logging
# DEBUG=true

# Optional: Custom temporary directory for file processing
# TEMP_DIR=/tmp/discord_bot

# Instructions:
# 1. Copy this file to .env: cp .env.template .env
# 2. Fill in your Discord bot token and OpenAI API key
# 3. Make sure Redis is running (or install with: brew install redis / apt-get install redis)
# 4. Start Redis: redis-server
# 5. Install dependencies: pip install -r requirements.txt
# 6. Run the bot: python tdd_bot.py